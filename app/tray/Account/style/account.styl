@keyframes arrowSlide
  0%
    opacity 0
    transform translateY(5px)
  20%
    opacity 1
  40%
    opacity 0
    transform translateY(-15px)
  100%
    opacity 0

@keyframes arrowSlideSingle
  0%
    opacity 0
    transform translateY(3px)
  50%
    opacity 1
  100%
    opacity 0
    transform translateY(-3px)

@keyframes headShake
  0%
    transform translateX(0)
  32%
    transform translateX(-3px)
  66%
    transform translateX(2px)
  100%
    transform translateX(0)

.headShake
  animation-duration var(--wren-motion-standard)
  animation-fill-mode both
  animation-timing-function var(--wren-ease)
  animation-name headShake
  transform translateZ(0)

.standardFade-enter
  opacity 0

.standardFade-enter.standardFade-enter-active
  opacity 1
  transition var(--standard)

.standardFade-leave
  opacity 1

.standardFade-leave.standardFade-leave-active
  opacity 0
  transition var(--standard)

.footerModule
  position absolute
  bottom 0px
  left 0px 
  right 0px
  min-height 40px
  z-index 99999999
  transition var(--standard)

  &:has(.requestNoticeApproval),
  &:has(.requestNoticeTransactionReview),
  &:has(.requestNoticeTransactionStatus)
    transition none

  .footerWrap
    width 100%
    display flow-root

.signer
  position relative
  z-index 3001
  transition var(--standard), 0s linear height
  will-change transform
  transform translate3d(0, 0, 0)
  pointer-events auto
  box-sizing border-box
  z-index 10000000

  .signerContainer
    position relative
    z-index 3002
    padding 0px 0px 5px 0px

.accountMain
  position absolute
  top 140px
  right 7px
  bottom 40px
  left 7px
  z-index 3
  display flex
  justify-content center
  align-items center
  border-radius 26px
  transition var(--standard)
  overflow hidden

  .accountsModuleExpand
    background var(--ghostZ)
    z-index 2001
    margin-bottom 30px
    height 100%

    .moduleExpanded
      z-index 20000

      .accountModuleInner
        position relative

    .accountModule
      position relative !important

  .accountMainScroll
    position absolute
    top 0
    right 0
    bottom 0
    left 0
    overflow-y scroll
    overflow-x hidden
    transform translate3d(0, 0, 0)
    border-radius 30px

  .accountMainSlide
    position relative 
    width 100%
    transform translate3d(0, 0, 0)
    transition var(--standardSlow)
    transition-delay 0.32s
    padding-bottom 90px
    pointer-events none
    z-index 999

  .signerAddress
    font-size var(--wren-type-caption)
    font-family 'FiraCode'
    letter-spacing 0px
    position absolute
    top 0px
    left 0px
    bottom 0px
    right 0px
    padding-top 0px
    transform translateZ(0)

    .transactionToAddressLargeWrap
      position absolute
      top 14px
      left 61px
      height 40px
      width 140px
      cursor pointer
      z-index 5

    .transactionToAddressLarge
      position absolute
      top 0px
      left 0px
      right 0px
      bottom 0px
      display flex
      font-size 17px
      opacity 1
      pointer-events none
      cursor pointer

      svg
        color var(--outerspace08)

    .transactionToAddressCopy
      display flex
      justify-content center
      align-items center
      font-size var(--wren-type-caption)
      letter-spacing 1px
      opacity 1
      pointer-events none
      text-transform uppercase
      font-weight 400
      letter-spacing 1px
      font-family 'MainFont'
      color var(--outerspace08)
      border-radius 10px
      transform translateY(0px)
      bottom 30px
      opacity 0

    .transactionToAddressFull
      position absolute
      top 2px
      right 0
      bottom 0
      left 0
      display flex
      justify-content center
      align-items center
      font-size 13.5px
      font-weight 300
      pointer-events none
      z-index 8
      cursor pointer

      .transactionToAddressFullCopied
        position absolute
        right 18px
        top 22px

        svg
          position relative
          top -1px
          margin-left 22px

    .transactionToAddressENS
      transform translateY(7px)

    .transactionToAddressFullHidden
      opacity 0

  .clickGuard
    position absolute
    top 0
    left 0
    bottom 0
    right 0
    z-index 4000
    border-radius 12px

  .accountListWrap
    position absolute
    top 100px
    left 0
    right 0
    height 0px
    background var(--ghostB)
    z-index 3015
    transition var(--standardFast)
    border-bottom-left-radius 16px
    border-bottom-right-radius 16px
    overflow hidden
    box-shadow 0px 2px 3px var(--thin), 0px 19px 9px -14px var(--thin) !important
    transform translateZ(0)
    display none

    .accountList
      position absolute
      left 0
      right 0
      bottom 0
      height 330px
      z-index 3018
      overflow hidden
      padding 15px 0px 10px 0px

      .settingsMenu
        background linear-gradient(var(--ghostB), var(--ghostD))
        display flex
        justify-content center
        flex-direction column
        align-items center
        position absolute
        left 0
        right 0
        bottom 0
        height 50px
        transition var(--standardSlow)
        opacity 1
        pointer-events auto
        .settingsMenuSelect
          position relative
          height 12px
          width 105px
          box-sizing border-box
          .settingsMenuMark
            height 12px
            position absolute
            transition var(--standard)
            .settingsMenuMarkLine
              position absolute
              background outerspace
              height 2px
              top 0px
              left 7px
              right 7px
              border-radius 1px
        .settingsMenuItems
          box-sizing border-box
          height 33px
          width 105px
          display flex
          .settingsMenuItem
            position relative
            box-sizing border-box
            z-index 1230
            width 35px
            height 33px
            color var(--outerspace08)
            .settingsMenuItemIcon
              top 0
              right 0
              bottom 0px
              left 0
              position absolute
              z-index 1234
              padding 10px 10px
          .settingsMenuItemSelected, .settingsMenuItem:hover
            color var(--outerspace)

      .accountListItems
        background var(--ghostB)
        height 280px
        min-height 280px
        width 100%
        display flex
        justify-content flex-start
        align-items center
        flex-direction column
        position relative
        z-index 3
        transition var(--standardSlow)
        transform translateY(-200px)
        pointer-events none

        .accountListItem
          background var(--ghostB)
          width calc(100% - 40px)
          height 55px
          min-height 55px
          border-bottom 1px solid var(--thin)
          display flex
          justify-content space-between
          align-items center
          padding 0px 20px 0px 20px
          font-family 'FiraCode'
          color var(--outerspace08)
          position relative
          z-index 4

          .accountListItemCheck
            color var(--outerspace01)
            margin-bottom 3px

          .accountListItemAddress
            svg
              margin 0px 4px 0px 4px

        .accountListItem:last-child
          border-bottom none

        .accountListItemSelected
          background var(--ghostC)
          color var(--outerspace)
          border-radius 6px
          border-bottom 1px solid var(--thick0)
          box-shadow 0px 2px 3px var(--thin), 0px 19px 9px -14px var(--thin) !important
          position relative
          z-index 10
          .accountListItemCheck
            color var(--outerspace)

      .accountPageToggle
        padding-top 20px
        height 20px
        min-height 20px
        padding-bottom 10px
        width 100%
        display flex
        justify-content center
        align-items center
        font-family 'FiraCode'
        position relative
        z-index 2
        transition var(--standardSlow)
        opacity 0
        pointer-events none
        transform translateY(-200px)

        .accountPageCurrent
          border-radius 3px
          height 27px
          width 27px
          display flex
          justify-content center
          align-items center
          font-size 18px

        .accountPageButton
          border-radius 3px
          height 27px
          width 27px
          display flex
          justify-content center
          align-items center
          box-sizing border-box

        .accountPageButtonLeft
          padding-right 1px

        .accountPageButtonRight
          padding-left 1px

        .accountPageButton:hover
          background var(--ghostD)

  .accountMenu
    position absolute
    top 64px
    right 0px
    left 0px
    border-radius 6px
    z-index 3005
    overflow-x hidden
    overflow-y hidden
    transition var(--standard), 0.1s linear background
    will-change transform
    background var(--ghostC)
    z-index 9999003
    height 40px
    background var(--ghostC)
    box-shadow 0px 4px 6px -1px var(--thick)
    animation-delay 0.2s
    .accountMenuLeft
      position absolute
      top 0px
      left 0px
      bottom 0px
      display flex
      .accountMenuItem
        border-right 1px solid var(--thick)
      .accountMenuItem:first-child
        padding-left 4px

    .accountMenuRight
      position absolute
      top 0px
      right 0px
      bottom 0px
      .accountMenuItem
        border-left 1px solid var(--thick)
      .accountMenuItem:last-child
        padding-right 4px
    
    .accountMenuItem
      height 40px
      width 40px
      display flex
      justify-content center
      align-items center
      cursor pointer

      div, svg 
        pointer-events none

    .accountMenuItem:hover
      background var(--ghostD)

  .signerMid
    position absolute
    top calc(22px + 83px)
    right 0px
    bottom 7px
    left 0px
    border-radius 6px
    z-index 3005
    overflow-x hidden
    overflow-y hidden
    opacity 1
    transition var(--standard), 0.1s linear background
    will-change transform
    background var(--ghostC)
    z-index 9999003
    box-shadow 0px 4px 6px -1px var(--thick)
    animation-delay 0.4s

    > div
      transition var(--standard)
      opacity 0

    .signerMidMenu
      background var(--thin)
      height 50px
      display flex
      flex-direction column
      justify-content center
      align-items center

      .signerMenuItems
        height 25px
        display flex
        justify-content center
        align-items center
        margin-top 5px

        .signerMenuItem
          height 30px
          width 40px
          display flex
          justify-content center
          align-items center

      .signerMenuSelect
        height 10px
        width 80px
        position relative

        .signerMenuSelectMarker
          position absolute
          left 0px
          height 2px
          width 20px
          // margin 0px 4px
          margin 0px 10px
          background black

    .signerNode
      display flex
      justify-content center
      align-items center
      font-size var(--wren-type-small)
      height 40px
      border-top 1px solid var(--ghostB)

  .signerEvents
    height 200px
    overflow scroll
    background rgba(0, 0, 50, 0.1)
    width 100%

  .signerSelectDown
    transform rotate(180deg)
    .signerSelectArrows
      .signerSelectArrow
        svg
          color var(--spacewhite)

  .signerSelectLeft
    left 3px

  .signerSelectRight
    right 3px

  .signerSelectDown.signerSelectLeft
    left -50px
    opacity 0

  .signerSelectDown.signerSelectRight
    right -50px
    opacity 0

  .signerReady
    font-size var(--wren-type-small)
    width 100%

.accountModule
  position absolute
  top 0
  left 2px
  right 2px
  transform translate3d(0, 0, 0)
  transition var(--standardFast)
  pointer-events auto
  padding-bottom 7px

.moduleExpanded
  min-height 100%
  .accountModuleInner
    background none
    box-shadow none
    border-radius none

  .accountLedgerView
    padding-right var(--wren-space-4)
    padding-left var(--wren-space-4)

    > ._txMain
      margin 0
      overflow visible
      border 0
      border-radius 0
      background transparent
      box-shadow none

    .cluster
      margin 0
      padding 0
      border 0
      border-radius 0
      background transparent

      .clusterRow
        gap 0

      .clusterRow + .clusterRow
        margin-top 0

      .clusterValue
        min-height 48px
        border 0
        border-radius 0
        background transparent

      .clusterValueClickable:not(.clusterValueDisabled):hover
        border-color transparent
        background var(--wren-ledger-hover)

      .clusterValueClickable:not(.clusterValueDisabled):active
        border-color transparent
        background var(--wren-surface-active)

    .moduleMainPermissions
      border 0
      border-radius 0
      background transparent

.requestsPreview
  appearance none
  display block
  width 100%
  padding 0
  border 0
  color inherit
  text-align left
  height 52px
  position relative
  font-size var(--wren-type-caption)
  text-transform uppercase
  letter-spacing 2px
  font-family 'MainFont'
  font-weight 500
  box-sizing border-box
  border-radius 26px
  cursor pointer
  overflow hidden
  background var(--ghostAZ)

  *
    pointer-events none

  .requestPreviewContent
    position absolute
    top 0
    right 0
    bottom 0
    left 0
    display flex
    align-items center
    justify-content space-between
    cursor pointer
    z-index 200
    padding 16px 0px 16px 20px
    box-sizing border-box
    overflow hidden
    border-radius 26px

    .requestPreviewContentTitle
      display flex
      align-items center

    .requestPreviewContentArrow
      display flex
      align-items center
      padding-right 20px
      transition var(--standard)

      div
        max-width 13px
        
  span
    margin-right 8px

  .requestsBadgeOverlay
    position absolute
    top 0
    right 0
    bottom 0
    left 0
    background linear-gradient(-35deg, var(--overlayA) 0%, var(--overlayB) 100%)
    z-index 999
    pointer-events none

.accountModuleInner
  position absolute
  top 0
  left 0px
  right 0px
  bottom 7px
  transform translate3d(0, 0, 0)
  padding 0px 6px
  border-radius 26px

  .accountModuleCard
    position relative
    border-radius 26px
    background var(--ghostAZ)
    box-shadow 0px 4px 8px var(--ghostY), 0px 2px 8px var(--ghostY)
    border-bottom 2px solid var(--ghostY)

  .moduleHeader:before
    content ''
    position absolute
    bottom -2px
    right 16px
    left 16px
    height 1px
    background var(--ghostA)

  .moduleHeaderBlank
    height 6px
    
  .moduleHeader
    position relative
    font-size var(--wren-type-caption)
    text-transform uppercase
    letter-spacing 2px
    padding 16px 90px 10px 20px
    height 42px
    font-family 'MainFont'
    font-weight 500
    box-sizing border-box
    display flex
    align-items center

    span
      margin-right 8px

    .moduleHeaderClose
      background var(--moon)
      color var(--moonOver)
      width 20px
      height 20px
      border-radius 10px
      position absolute
      right 11px
      top 9px
      bottom 0
      display flex
      justify-content center
      align-items center
      cursor pointer
      *
        pointer-events none

    .moduleHeaderLoading
      position absolute
      left 6px
      top 1px
      bottom 0
      // padding 0px 10px
      display flex
      justify-content center
      align-items center
      
    .moduleHeaderLoadingLoader
      border-radius 50%
      width 12px
      height 12px
      margin 12px auto
      font-size 10px // Spinner geometry; no visible text.
      position relative
      text-indent -9999em
      border-top 3px solid var(--ghostZ)
      border-right 3px solid var(--ghostZ)
      border-bottom 3px solid var(--ghostZ)
      border-left 3px solid var(--good)
      transform translateZ(0)
      animation load 0.5s infinite linear
      will-change transform

  .moduleHeader:before
    background none

  .moduleHeaderBorderless
    border-bottom none

  //.moduleButtonBad:hover
    // border 1px solid var(--bad)
    /// background var(--bad01)

  .moduleMain
    padding 16px

  .moduleFooter
    padding 16px

  .moduleMainSettings
    font-size var(--wren-type-caption)
    text-transform uppercase
    font-weight 400

  // .moduleMid
  //   padding-bottom 42px

  .moduleFooterAction
    position absolute
    bottom 0px
    width 100%
    cursor pointer
    text-align center
    font-size var(--wren-type-small)
    text-transform uppercase
    border-top 1px solid var(--ghostA)
    box-sizing border-box
    font-weight 300
    letter-spacing 1px
    padding 12px 
    height 42px
    text-align center
    box-sizing border-box
    background var(--ghostB)
    z-index 400000
    
  .moduleFooterAction:hover
    background var(--ghostD)


    // .moduleMain
    //   padding 8px

.signerVerifyText
  font-size 15px
  text-align center
  display flex
  justify-content center
  align-items center
  height 70px
  padding 0px 30px

.moduleRow
  height 30px
  margin-bottom 4px
  display flex
  align-items center

  input
    z-index 200
    user-select auto
    background var(--ghostC)
    outline none
    border none
    margin-left 8px
    height 100%
    color var(--outerspace)
    border-radius 8px
    padding 0px 8px

.moduleButton
  width 100%
  height 40px
  background var(--ghostAZ)
  border-radius 12px
  color var(--outerspace)
  display flex
  justify-content center
  align-items center
  font-weight 400
  font-size 13px
  box-sizing border-box
  text-transform uppercase
  box-shadow 0px 1px 2px var(--ghostZ)
  
  svg
    position relative

  span
    padding-left 4px

  *
    pointer-events none

.moduleButton.signerVerifyInProgress
  background var(--ghostC)
  color var(--outerspace04)

.moduleButton:hover:not(.signerVerifyInProgress)
  cursor pointer
  background var(--ghostD)
  box-shadow 0px 4px 6px var(--ghostA)

.moduleButtonBad
  color var(--bad)

.moduleButtonGood
  color var(--good)

.moduleButtonLocked
  color var(--ghostD)

.moduleButtonLocked:hover
  background var(--ghostB)
  box-shadow 0px 2px 2px var(--ghostA)

@import '../Balances/style'
@import '../Activity/style'
@import '../Inventory/style'
@import '../Permissions/style'
@import '../Requests/style'
@import '../Settings/style'
@import '../Signer/style'
@import '../WalletCallsStatus/style'

@import './view'

// Wren account surface
.accountMain
  top 88px
  right 0
  left 0
  border-radius 0

  .accountMainScroll
    border-radius 0

  .accountMainSlide
    padding-top var(--wren-space-4)
    padding-right var(--wren-space-5)
    padding-left var(--wren-space-5)
    box-sizing border-box

.accountModule
  right var(--wren-space-5)
  left var(--wren-space-5)
  padding-bottom 0

.accountModuleInner
  right 0
  bottom 0
  left 0
  padding 0
  border-radius var(--wren-radius-sm)

  .accountModuleCard
    overflow hidden
    border 0
    border-top 0
    border-radius 0
    background transparent
    box-shadow none

    // Preview modules use spacing and whole-row feedback instead of card outlines.
    .cluster
      margin 0
      padding 0
      border 0
      border-radius 0
      background transparent

      .clusterRow
        gap 0

      .clusterRow + .clusterRow
        margin-top var(--wren-space-2)

      .clusterValue
        border 0
        border-radius 0
        background transparent

      .clusterValueClickable:not(.clusterValueDisabled):hover
        border-color transparent
        background var(--wren-ledger-hover)

      .clusterValueClickable:not(.clusterValueDisabled):active
        border-color transparent
        background var(--wren-surface-active)

    // The compact account illustrations are intentionally opaque RGB assets.
    // Match their charcoal field to the ledger and feather the crop without
    // dimming transparent empty-state artwork on other surfaces.
    .wrenEmptyState
      background transparent

    .wrenEmptyStateImage
      opacity .9
      filter brightness(.7) saturate(.92)
      -webkit-mask-image radial-gradient(ellipse 66% 57% at 52% 52%, #000 32%, rgba(0, 0, 0, .82) 55%, transparent 88%)
      mask-image radial-gradient(ellipse 66% 57% at 52% 52%, #000 32%, rgba(0, 0, 0, .82) 55%, transparent 88%)

    .wrenEmptyStateImageTransparent
      opacity 1
      filter none
      -webkit-mask-image none
      mask-image none

    .wrenEmptyStateTransparent:not(.wrenEmptyStateExpanded)
      min-height 96px

      .wrenEmptyStateImageTransparent
        top 45%
        width 200px

    .accountLedgerModule
      .accountLedgerRow
        min-height 52px

      .settingsPreviewRemovalConfirm > .clusterValue:last-child
        min-height 42px
        margin 5px var(--wren-space-2)
        border 1px solid rgba(225, 154, 145, .42)
        border-radius var(--wren-radius-sm)
        color var(--wren-danger)
        background var(--wren-danger-soft)
        background-image var(--wren-control-texture-dark)
        box-shadow var(--wren-control-shadow)
        transition background var(--wren-motion-fast) var(--wren-ease), border-color var(--wren-motion-fast) var(--wren-ease), box-shadow var(--wren-motion-fast) var(--wren-ease), transform var(--wren-motion-fast) var(--wren-ease)

      .settingsPreviewRemovalConfirm > .clusterValue:last-child:not(.clusterValueDisabled):hover
        border-color var(--wren-danger)
        background var(--wren-danger-soft)
        background-image var(--wren-control-texture-dark)
        box-shadow var(--wren-control-shadow-hover)

      .settingsPreviewRemovalConfirm > .clusterValue:last-child:not(.clusterValueDisabled):active
        background var(--wren-danger-soft)
        box-shadow var(--wren-control-shadow-pressed)
        transform translateY(1px)

      .accountLedgerLabel
        display grid
        flex 0 0 112px
        min-width 112px
        padding 0 0 0 var(--wren-account-ledger-inset)
        box-sizing border-box
        align-items center
        grid-template-columns var(--wren-account-ledger-mark-column) minmax(0, 1fr)
        column-gap var(--wren-account-ledger-column-gap)
        color var(--wren-text-tertiary)
        font-family var(--wren-font-ui)
        font-size var(--wren-type-small)
        font-weight 560
        line-height 18px

        > span:first-child
          display flex
          align-items center
          justify-content flex-start

        svg
          display block
          color var(--wren-text-secondary)

      .clusterValue
        min-height 52px

      .moduleItem
        min-height 52px
        padding var(--wren-space-3)

      .signerPreviewSummary
        min-height 52px
        padding 0 var(--wren-space-3)

      .signerPreviewRow
        .moduleItemSignerType,
        .signerPreviewStatus
          line-height 18px

        .moduleItemIcon
          display flex
          height 18px
          align-items center
          justify-content center

          svg
            display block

        .signerPreviewVerify svg
          display block

.accountMainSlide .moduleHeader
  height 40px
  padding 13px 76px 10px var(--wren-space-4)
  color var(--wren-text-tertiary)
  font-family var(--wren-font-ui)
  font-size var(--wren-type-small)
  font-variation-settings var(--wren-font-heading-settings)
  font-weight 590
  letter-spacing .1px
  text-transform none

.accountMainSlide > .accountModule:first-child
  .moduleMain
    padding var(--wren-space-4)

  .moduleFooterAction
    margin-top var(--wren-space-2)
    color var(--wren-text-secondary)
    background transparent
    font-size var(--wren-type-small)
    text-transform none

    &:hover
      color var(--wren-text-primary)
      background var(--wren-surface-hover)

.requestsPreview
  height 48px
  border 0
  border-radius 0
  color var(--wren-text-secondary)
  background transparent
  font-size var(--wren-type-small)
  letter-spacing .4px
  text-transform none
  transition background var(--wren-motion-fast) var(--wren-ease)

  &:hover
    color var(--wren-text-primary)
    background var(--wren-surface-hover)

  .requestsPreviewOverlay,
  .requestsPreviewArrow
    display none

  .requestPreviewContent
    padding 0 var(--wren-space-4)
    border-radius var(--wren-radius-sm)

    .requestPreviewContentTitle
      font-family var(--wren-font-ui)
      font-variation-settings var(--wren-font-heading-settings)
      font-weight 590
      letter-spacing .1px

    .requestPreviewContentEnd
      display flex
      min-width 0
      align-items center
      gap var(--wren-space-3)

    .requestPreviewContentMeta
      min-width 0
      overflow hidden
      margin 0
      color var(--wren-text-muted)
      font-family var(--wren-font-utility)
      font-size var(--wren-type-caption)
      font-weight 450
      letter-spacing 0
      text-overflow ellipsis
      white-space nowrap

    .requestPreviewContentArrow
      flex 0 0 auto
      padding-right 0

.accountView
  right 0
  left 0
  border-radius 0

  .accountViewMenu
    top 0
    right 0
    left 0
    height 64px
    padding 0 var(--wren-space-5)
    box-sizing border-box
    border 0
    border-bottom 1px solid var(--wren-border-subtle)
    border-radius 0
    background transparent
    box-shadow none

    .accountViewTitle
      color var(--wren-text-secondary)
      font-family var(--wren-font-ui)
      font-size var(--wren-type-small)
      font-weight 500
      letter-spacing .2px
      text-transform none

    .accountViewBack
      top 12px
      left 12px

  .accountViewMain
    top calc(64px + var(--wren-space-5))
    right var(--wren-space-5)
    left var(--wren-space-5)
    padding-top 0

    .accountViewScroll
      padding-top 0

  &:has(.requestViewScroll)
    .accountViewMain
      top 64px

  &:has(.signerRequest)
    .accountViewMain
      top 64px

  &.accountViewCompact
    .accountViewMenu
      top 0

  &.accountViewRequest
    .accountViewMenu
      top 0
      right 0
      left 0

      .accountViewTitle
        right 176px
        left 56px
        justify-content flex-start
        color var(--wren-text-primary)
        font-size 20px
        font-variation-settings var(--wren-font-heading-settings)
        font-weight 650
        line-height 1

        .accountViewText
          font-family inherit
          font-size inherit
          font-weight inherit

      .accountViewMeta
        position absolute
        top 10px
        right 0
        display block
        max-width 152px
        height 32px
        box-sizing border-box
        overflow hidden
        color var(--wren-text-tertiary)
        font-family var(--wren-font-utility)
        font-size var(--wren-type-caption)
        line-height 32px
        text-align right
        text-overflow ellipsis
        white-space nowrap

    .accountViewMain
      top 64px
      right 0
      left 0

  .cardShow
    animation none
    opacity 1
    transform none

.moduleExpanded
  padding-bottom var(--wren-space-5)

  .accountModuleInner
    padding 0

  .accountModuleCard
    overflow hidden
    border 1px solid var(--wren-border-default)
    border-radius 10px
    background var(--wren-surface-card)
    box-shadow var(--wren-shadow-sm), var(--wren-shadow-inset)

  .accountLedgerView
    padding-right var(--wren-space-3)
    padding-left var(--wren-space-3)

// Account home follows the approved controls study: the selected-account switcher
// owns the header, so the startup dashboard control stays out of the selected state.
#panel
  &:has(.requestNoticeApproval) .accountViewRequest,
  &:has(.requestNoticeTransactionReview) .accountViewRequest
    transition none

  &:has(.requestNoticeTransactionReview)
    --wren-request-review-shelf-height 88px

    .accountViewRequest
      bottom var(--wren-request-review-shelf-height) !important

    .footerModule
      height var(--wren-request-review-shelf-height) !important

  &:has(.requestNoticeTransactionStatus)
    --wren-request-status-shelf-height 88px

    .accountViewRequest
      bottom var(--wren-request-status-shelf-height) !important

    .footerModule
      height var(--wren-request-status-shelf-height) !important

  &:has(.requestNoticeTransactionUnconfirmed)
    --wren-request-status-shelf-height 114px

  &:has(.requestNoticeApproval)
    --wren-request-warning-shelf-height 154px

    .accountViewRequest
      bottom var(--wren-request-warning-shelf-height) !important

    .footerModule
      height var(--wren-request-warning-shelf-height) !important

  &:has(.eip7702StopMonitoringDialog)
    --wren-request-dialog-shelf-height 173px

    .accountViewRequest
      bottom var(--wren-request-dialog-shelf-height) !important

    .footerModule
      height var(--wren-request-dialog-shelf-height) !important

  &:has(.walletCallsReview)
    --wren-wallet-calls-shelf-height 114px

    .accountViewRequest
      bottom var(--wren-wallet-calls-shelf-height) !important
      transition none

    .footerModule
      height var(--wren-wallet-calls-shelf-height) !important

  &:has(.walletCallsFundingRecovery)
    --wren-wallet-calls-recovery-shelf-height 165px

    .accountViewRequest
      bottom var(--wren-wallet-calls-recovery-shelf-height) !important

    .footerModule
      height var(--wren-wallet-calls-recovery-shelf-height) !important

  &:has(.walletCallsFundingRecoveryEvidence)
    --wren-wallet-calls-recovery-shelf-height 277px

  &:has(.walletCallsFundingRecoveryQr)
    --wren-wallet-calls-recovery-shelf-height 430px

  &:has(.accountSelectorOpen) .panelMenuItemOpen
    display none

  &:has(.accountView) .accountSelector.accountSelectorOpen
    display none

  &:has(.accountSelectorOpen[aria-modal='true']) .accountMain
    display none

// Populated wallet views share one restrained palette. The tray shell owns the
// wallet atmosphere; these views only refine content roles.
#panel:has(.accountSelector:not(.accountSelectorOpen)),
#panel:has(.accountMainPerch),
#panel:has(.balancesExpandedView),
#panel:has(.activityModuleExpanded),
#panel:has(.permissionsLedgerView),
#panel:has(.accountSettingsExpandedView),
#panel:has(.requestViewScroll)
  --wren-bg-elevated #121813
  --wren-surface-card rgba(13, 18, 16, .66)
  --wren-border-subtle rgba(150, 182, 150, .10)
  --wren-border-default rgba(150, 182, 150, .16)
  --wren-border-strong rgba(150, 182, 150, .22)
  --wren-text-primary #f2efe8
  --wren-text-secondary #d6d2c8
  --wren-text-tertiary #97917f
  --wren-text-muted #97917f
  --wren-text-disabled #6d7568
// Focused signing uses one opaque canvas through the review bar and body,
// without carrying the ordinary wallet's atmospheric color fields into it.
#panel:has(.accountViewRequest),
#panel:has(.walletCallsStatus)
  --wren-window-mode-image var(--wren-window-atmosphere-focus)
  --wren-window-mode-repeat var(--wren-window-atmosphere-focus-repeat)
  --wren-window-mode-size var(--wren-window-atmosphere-focus-size)
  --wren-window-mode-blend var(--wren-window-atmosphere-focus-blend)
  --wren-window-mode-shadow var(--wren-window-atmosphere-focus-shadow)

@media (max-width: 560px)
  #panel:has(.requestNoticeTransactionReview)
    --wren-request-review-shelf-height 132px

.accountMain.accountMainPerch
  --wren-account-ledger-inset var(--wren-space-4)
  --wren-account-ledger-mark-column 32px
  --wren-account-ledger-column-gap var(--wren-space-2)
  --wren-account-ledger-text-offset calc(var(--wren-account-ledger-mark-column) + var(--wren-account-ledger-column-gap))
  top 64px
  right 0
  bottom 0 !important
  left 0
  display block
  border-radius 0
  overflow hidden
  background transparent

  .accountMainScroll
    top 0
    right 0
    bottom 0
    left 0
    padding 2px 18px 16px
    scroll-padding-bottom 40px
    box-sizing border-box
    border-radius 0
    &::-webkit-scrollbar
      width 8px

    &::-webkit-scrollbar-thumb
      border 0
      border-radius 4px
      background rgba(150, 182, 150, .22)

  .accountMainSlide
    width 100%
    margin-top 12px
    padding 0
    padding-bottom 0
    box-sizing border-box

.accountScrollFade
  position absolute
  right 8px
  bottom 0
  left 0
  z-index 19999999
  height 32px
  opacity 0
  background linear-gradient(to bottom, rgba(7, 9, 7, 0), rgba(7, 9, 7, .26) 52%, rgba(7, 9, 7, .78))
  transition opacity var(--wren-motion-fast) var(--wren-ease)
  pointer-events none

.accountScrollFadeVisible
  opacity 1

.accountPortfolioCard
  position relative
  height 144px
  padding 15px var(--wren-account-ledger-inset) 18px
  box-sizing border-box
  overflow hidden
  border 1px solid var(--wren-border-default)
  border-radius 10px
  background linear-gradient(172deg, rgba(255, 255, 255, .022), transparent 46%), rgba(13, 18, 16, .66)
  box-shadow 0 10px 26px rgba(0, 0, 0, .5), inset 1px 1px 0 rgba(150, 182, 150, .05), inset 0 1px 0 rgba(255, 255, 255, .05)

.accountPortfolioGlow
  position absolute
  top -30px
  left 50%
  width 360px
  height 210px
  background radial-gradient(58% 62% at 50% 42%, rgba(168, 116, 63, .16), rgba(120, 84, 54, .05) 46%, transparent 72%)
  transform translateX(-50%)
  pointer-events none

.accountPortfolioHeader
  position relative
  z-index 1
  display flex
  align-items center
  justify-content space-between

.accountMainPerch .accountPortfolioHeader,
.accountMainPerch .requestPreviewHeading,
.accountMainPerch .moduleHeader
  color var(--wren-text-tertiary)
  font-family var(--wren-font-mono)
  font-size 12px
  font-weight 600
  line-height 18px
  letter-spacing 1.4px
  text-transform uppercase

.accountPortfolioValue
  position relative
  z-index 1
  margin-top 28px
  color var(--wren-text-primary)
  font-family var(--wren-font-ui)
  font-size 40px
  font-weight 400
  line-height 1
  letter-spacing -.7px

.accountPortfolioMeta
  position relative
  z-index 1
  margin-top 9px
  color var(--wren-text-secondary)
  font-size 14px
  font-weight 420

.accountPortfolioSend
  position absolute
  z-index 2
  right 16px
  bottom 24px
  width 136px
  height 44px
  min-height 44px
  padding 0 16px
  border-radius 9px
  font-size 13px
  font-weight 620

.accountMainPerch .accountModule
  right 0
  left 0
  padding 0

.accountMainPerch .accountModuleInner
  right 0
  bottom 0
  left 0
  padding 0
  border-radius 10px

  .accountModuleCard
    height 100%
    box-sizing border-box
    overflow hidden
    border 1px solid var(--wren-border-default)
    border-radius 10px
    background linear-gradient(172deg, rgba(255, 255, 255, .022), transparent 46%), rgba(13, 18, 16, .66)
    box-shadow 0 10px 26px rgba(0, 0, 0, .5), inset 1px 1px 0 rgba(150, 182, 150, .05), inset 0 1px 0 rgba(255, 255, 255, .05)

.accountMainPerch .moduleHeader
  display flex
  height 48px
  padding 15px var(--wren-account-ledger-inset)
  box-sizing border-box
  align-items center

  &:before
    display none

  > span:first-child
    display none

.accountMainPerch .activityModuleEmpty .moduleHeader,
.accountMainPerch .permissionsModuleHeader
  height 36px
  padding-bottom 3px

.accountMainPerch .requestsPreview
  height auto
  min-height 106px
  border 0
  border-radius 0
  color inherit
  background transparent
  font-family var(--wren-font-ui)
  text-transform none
  letter-spacing 0

  &:hover
    background transparent

  .requestPreviewContent
    position relative
    inset auto
    display block
    min-height 106px
    padding 15px 16px 16px
    box-sizing border-box
    border-radius 0

  .requestPreviewHeading
    display flex
    align-items center
    justify-content space-between

    span
      margin 0

    span:last-child
      color #6d7568
      font-weight 400
      letter-spacing 0
      text-transform none

  .requestPreviewItem
    display flex
    min-height 62px
    margin-top 22px
    padding 9px 12px
    box-sizing border-box
    align-items center
    gap 12px
    border 1px solid var(--wren-border-default)
    border-radius 10px
    background #0c100d

  .requestPreviewIdentity
    display flex
    min-width 0
    margin 0
    flex 1
    flex-direction column
    gap 2px

    strong
      overflow hidden
      color var(--wren-text-primary)
      font-size 14px
      font-weight 560
      text-overflow ellipsis
      white-space nowrap

    span
      overflow hidden
      margin 0
      color var(--wren-text-tertiary)
      font-family var(--wren-font-mono)
      font-size 12px
      text-overflow ellipsis
      white-space nowrap

  .requestPreviewReview
    margin 0
    flex none
    color #c08d54
    font-family var(--wren-font-mono)
    font-size 12px
    font-weight 600
    letter-spacing .4px

  .requestPreviewEmpty
    margin-top 12px
    padding 14px 12px
    color var(--wren-text-tertiary)
    border 1px solid var(--wren-border-subtle)
    border-radius 10px
    background #0c100d
    font-size 13px

  .requestPreviewContentEnd
    display none

.accountMainPerch .chainMonitorPreview
  padding 10px 14px
  box-sizing border-box

.chainMonitorCompactRow
  display grid
  min-height 44px
  align-items center
  gap 12px
  grid-template-columns auto minmax(0, 1fr) auto

.chainMonitorLabel
  color var(--wren-text-tertiary)
  font-family var(--wren-font-mono)
  font-size 12px
  font-weight 600
  letter-spacing 1.4px
  text-transform uppercase

.chainMonitorSummary
  display flex
  min-width 0
  align-items center
  gap 7px
  color var(--wren-text-tertiary)
  font-family var(--wren-font-mono)
  font-size 12px

  > strong
    overflow hidden
    color var(--wren-text-primary)
    font-family var(--wren-font-ui)
    font-size 13px
    font-weight 560
    text-overflow ellipsis
    white-space nowrap

  .sliceTileGasPrice
    color var(--wren-text-primary)
    font-family var(--wren-font-mono)
    font-size 12px

.chainMonitorControls
  display flex
  align-items center
  gap 2px

  button.wrenControl
    width 44px
    min-width 44px
    height 44px
    min-height 44px
    padding 0
    border-radius 8px

  button.chainMonitorDisclosure
    width auto
    min-width 90px
    padding 0 9px

.accountMainPerch .balancesPreview
  overflow visible

  > .cluster
    margin 0 16px
    padding 0
    border 0
    background transparent

    .clusterRow
      margin 0
      border-top 1px solid var(--wren-border-subtle)

      &:first-child
        border-top 0

    .clusterValue
      min-height 62px
      border 0
      border-radius 0
      background transparent

  .signerBalanceTotal
    height 60px
    margin 0 8px 8px
    padding 8px 0
    box-sizing border-box

.accountMainPerch .signerBalance
  height 62px

  .signerBalanceIcon
    top 15px
    left 0
    width var(--wren-account-ledger-mark-column)
    height var(--wren-account-ledger-mark-column)
    overflow visible
    box-sizing border-box
    border 0
    border-radius 0
    background transparent

    .balancesAssetMark
      --asset-mark-vector-size 20px

      .ringIconFallback
        font-size 13px
        transform translate(calc(-50% + 3px), -50%)

  .signerBalanceCurrency
    top 12px
    right 48%
    left var(--wren-account-ledger-text-offset)
    height 18px
    font-size 14px
    font-weight 500

  .signerBalanceChain
    top 34px
    left var(--wren-account-ledger-text-offset)
    height 14px
    color var(--wren-text-tertiary) !important
    font-family var(--wren-font-mono)
    font-size 12px
    font-weight 400
    letter-spacing 0

  .signerBalanceValue
    top 11px
    right 0
    left 52%
    height 18px
    font-size 13px

  .signerBalancePrice
    right 0
    bottom 10px
    left 52%
    justify-content flex-end
    font-size 12px

  .signerBalanceOk
    display none

.accountMainPerch .accountModule-permissions .connectedAppsList
  .signerPermission
    padding-left var(--wren-account-ledger-inset)

  .signerPermissionControls
    display grid
    grid-template-columns var(--wren-account-ledger-mark-column) minmax(0, 1fr) auto
    column-gap var(--wren-account-ledger-column-gap)

  .signerPermissionIdentity
    width auto
    min-width 0

.accountMainPerch .accountModule-activity,
.accountMainPerch .accountModule-permissions,
.accountMainPerch .accountModule-signer,
.accountMainPerch .accountModule-settings
  .accountModuleCard > div
    box-sizing border-box

.accountMainPerch .accountModule-settings .accountModuleCard
  border 0
  background transparent
  box-shadow none

@media (max-height: 760px)
  .accountMain.accountMainPerch .accountMainScroll
    bottom 0

  .accountHomeShelf
    height 68px
    padding-top 8px
    padding-bottom 8px

.accountHomeHeader
  display grid
  padding 0 var(--wren-space-5) var(--wren-space-3)
  margin-bottom var(--wren-space-2)
  grid-template-columns minmax(0, 1fr) auto
  column-gap var(--wren-space-5)
  align-items start

.accountHomeIdentity
  min-width 0

.accountHomeTitle
  width 100%
  min-width 0
  margin 0
  overflow hidden
  color var(--wren-text-primary)
  font-family var(--wren-font-ui)
  font-size 25px
  font-weight 660
  line-height 1.08
  text-overflow ellipsis
  white-space nowrap

.accountHomeNameRow
  display flex
  min-width 0
  min-height 32px
  margin-top 0
  align-items center
  gap var(--wren-space-2)

button.accountHomeRename.wrenControl
  display flex
  max-width 100%
  min-width 0
  min-height 44px
  height auto
  padding 0 var(--wren-space-1)
  align-items center
  gap var(--wren-space-2)
  color inherit
  font inherit
  letter-spacing inherit
  text-transform none

  svg
    flex none
    color var(--wren-text-muted)

  &:hover svg
    color var(--wren-text-secondary)

.accountHomeNameText
  min-width 0
  overflow hidden
  text-overflow ellipsis
  white-space nowrap

.accountHomeNameInput
  width min(320px, 100%)
  min-width 180px
  height 32px
  min-height 32px
  padding 0 var(--wren-space-3)
  box-sizing border-box
  font-size 18px
  font-weight 620

.accountHomeAddressActions
  position relative
  display flex
  max-width 100%
  min-height 44px
  margin-top 3px
  align-items center
  gap var(--wren-space-1)

.accountHomeQrTrigger
  width 44px !important
  min-width 44px !important
  height 44px !important
  min-height 44px !important

  &[aria-expanded='true']
    color var(--wren-text-primary)
    border-color var(--wren-border-subtle)
    background-color var(--wren-surface-active)

.accountHomeQrDisclosure
  position relative
  flex none
  width 44px
  height 44px

.accountAddressQrPopover
  position absolute
  top calc(100% + var(--wren-space-1))
  left 50%
  z-index 20001
  width 217px
  margin-left -108.5px
  padding var(--wren-space-3)
  box-sizing border-box
  border 1px solid var(--wren-border-default)
  border-radius var(--wren-radius-md)
  color var(--wren-text-primary)
  background-color var(--wren-bg-elevated)
  background-image var(--wren-control-texture-dark)
  box-shadow var(--wren-shadow-lg), var(--wren-shadow-inset)
  animation cardShow var(--wren-motion-standard) var(--wren-ease) both

  &::before
    content ''
    position absolute
    top -5px
    left calc(50% - 5px)
    width 9px
    height 9px
    border-top 1px solid var(--wren-border-default)
    border-left 1px solid var(--wren-border-default)
    background var(--wren-bg-elevated)
    transform rotate(45deg)

.accountAddressQrHeader
  display flex
  min-height 38px
  margin-bottom var(--wren-space-2)
  align-items flex-start
  justify-content space-between
  gap var(--wren-space-2)

.accountAddressQrTitle
  margin 0
  color var(--wren-text-primary)
  font-family var(--wren-font-ui)
  font-size var(--wren-type-body)
  font-weight 650
  line-height 1.2

.accountAddressQrAccount
  max-width 135px
  margin-top 3px
  overflow hidden
  color var(--wren-text-muted)
  font-family var(--wren-font-ui)
  font-size var(--wren-type-caption)
  line-height 1.25
  text-overflow ellipsis
  white-space nowrap

.accountAddressQrCode
  display block
  width 185px
  max-width 100%
  height auto
  margin 0 auto

.accountAddressQrValue
  margin-top var(--wren-space-2)
  overflow-wrap anywhere
  color var(--wren-text-secondary)
  font-family var(--wren-font-mono)
  font-size var(--wren-type-caption)
  line-height 1.45

.accountHomeAddress
  appearance none
  display inline-flex
  min-width 0
  min-height 44px
  max-width 100%
  padding 0 var(--wren-space-1)
  align-items center
  gap 7px
  overflow hidden
  border 0
  color var(--wren-text-muted)
  background transparent
  cursor pointer
  font-family var(--wren-font-mono)
  font-size var(--wren-type-caption)
  text-align left
  transition color var(--wren-motion-fast) var(--wren-ease)

  span
    overflow hidden
    text-overflow ellipsis
    white-space nowrap

  svg
    flex none

  &:hover
    color var(--wren-text-secondary)

  &:active
    color var(--wren-accent-primary)

  &:focus-visible
    border-radius 2px
    outline 2px solid var(--wren-focus)
    outline-offset 3px

.accountHomeActions
  display flex
  padding-top var(--wren-space-3)
  align-items center
  justify-content flex-end
  gap var(--wren-space-2)

  .wrenControl
    svg
      flex none

.accountContinuationRow
  appearance none
  display flex
  width calc(100% - var(--wren-space-4))
  min-height 44px
  margin var(--wren-space-1) var(--wren-space-2) 0
  padding 0 var(--wren-space-3)
  box-sizing border-box
  align-items center
  justify-content space-between
  gap var(--wren-space-3)
  border 0
  border-radius var(--wren-radius-sm)
  color var(--wren-text-secondary)
  background transparent
  cursor pointer
  font-family var(--wren-font-ui)
  font-size var(--wren-type-small)
  font-weight 540
  text-align left
  transition color var(--wren-motion-fast) var(--wren-ease), background var(--wren-motion-fast) var(--wren-ease)

  svg
    flex none
    color var(--wren-text-tertiary)
    transition color var(--wren-motion-fast) var(--wren-ease), transform var(--wren-motion-fast) var(--wren-ease)

  &:hover:not(:disabled)
    color var(--wren-text-primary)
    background var(--wren-surface-hover)

    svg
      color var(--wren-accent-primary)
      transform translateX(1px)

  &:active:not(:disabled)
    background var(--wren-surface-active)

  &:focus-visible
    outline 2px solid var(--wren-focus)
    outline-offset -2px

  &:disabled
    cursor default
    opacity .58

.accountContinuationRow.activityContinuation
  margin-top var(--wren-space-2)
  margin-bottom var(--wren-space-2)
  padding-left var(--wren-space-5)

.accountContinuationRow.connectedAppsContinuation
  padding-left var(--wren-space-5)
