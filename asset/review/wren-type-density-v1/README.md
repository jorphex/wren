# Wren type density check — review v1

**Purpose:** qualify the functional type floor after the inherited tiny-text migration at 760 × 900.

**Status — selected:** corrected hybrid. Recursive owns headings, prose, row labels, and controls;
Fira Code is limited to compact utility roles. VCR is excluded from production.

- **Evidence-only:** `01-functional-type-density.png` and `type-density-check.html` are the 1× density
  and clipping check, not a layout proposal.
- **Selected evidence:** `03-hybrid-pixel-accent-directions.png` compares corrected hybrid, VCR-led,
  conservative data mono, and restricted VCR display use.
- **Source:** `hybrid-pixel-accent-check.html` reproduces that comparison.

Visible functional copy uses 12px or larger. Text-indented spinner geometry remains exempt because it
does not render copy.

VCR's small-size legibility failed earlier evaluation; it remains review-only pending provenance for
any restricted future use. The intermediate texture plate was superseded and removed.
